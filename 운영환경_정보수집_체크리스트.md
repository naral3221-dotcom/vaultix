# 운영 환경 정보 수집 체크리스트

> Phase 0 기반 셋업 상세 기획서를 작성하기 전에 채워주세요.
> 모르는 항목은 "모름" 또는 "확인필요"로 남겨주시면, 코워크에서 같이 조사/결정합니다.
> 작성 후 코워크 채팅에 "체크리스트 채웠어" 한 마디 + 이 파일이 업데이트되었음을 알려주세요.

---

## 1. VPS (운영 서버) 사양

| 항목 | 값 | 비고 |
|---|---|---|
| 제공 업체 (Vultr / Hetzner / Contabo / 자체 등) | | |
| 위치 (리전) | | 한국 트래픽 비중 / 글로벌 비중에 영향 |
| CPU 코어 수 | | |
| CPU 모델 (가능하면) | | `lscpu` 또는 `cat /proc/cpuinfo` |
| RAM (GB) | | `free -h` |
| 스왑 설정 여부 | | |
| 디스크 종류 (NVMe / SSD / HDD) | | |
| 디스크 총 용량 / 현재 사용량 | | `df -h` |
| GPU 보유 여부 | | Y/N |
| GPU 모델 / VRAM | | 있다면 (예: RTX 4060 8GB) |
| CUDA 버전 | | `nvidia-smi` |
| 월 비용 (참고용) | | 예산 의사결정 참고 |

> **참고**: Vaultix MVP는 이미지 생성 기본 라인을 Nanobanana API → OpenAI `gpt-image-2`로 둡니다. VPS GPU가 없더라도 진행 가능하며, 데스크탑 ComfyUI는 특수 워크플로우 보조로만 검토합니다.

---

## 2. OS / 시스템 환경

| 항목 | 값 | 비고 |
|---|---|---|
| OS 종류 + 버전 | | 예: Ubuntu 22.04 LTS, Debian 12 |
| 커널 버전 | | `uname -r` |
| Docker 버전 | | `docker --version` |
| Docker Compose 버전 | | `docker compose version` (v2 권장) |
| 시간대 (timezone) | | `timedatectl` |
| sudo 권한 사용자 | | 본인 계정명 |
| SSH 접속 방식 | | 비밀번호 / 키 (포트 변경 여부) |
| 방화벽 (ufw/firewalld) 사용 여부 | | |

---

## 3. 기존 운영 중인 서비스 (포트 충돌 점검)

같은 VPS에서 이미 돌고 있는 서비스가 있다면 적어주세요. 포트 충돌과 자원 분담 검토에 필수입니다.

| 서비스명 | 사용 포트 | 컨테이너/베어메탈 | 비고 |
|---|---|---|---|
| 예: Nginx Proxy Manager | 80, 443, 81 | Docker | |
|  |  |  |  |
|  |  |  |  |

추가로 알려주세요:
- 80/443 포트를 점유 중인 리버스 프록시가 이미 있나요? (있다면 이름: ___________ )
- PostgreSQL 인스턴스가 이미 운영 중인가요? (Y/N, 포트 ______)
- 이미 사용 중인 도커 네트워크 이름이 있나요? __________

---

## 4. 도메인 / DNS

| 항목 | 값 |
|---|---|
| 현재 보유 중인 도메인 (있다면) | |
| 선호 도메인 등록 대행사 | 가비아 / Namecheap / Cloudflare Registrar / 기타 |
| Cloudflare 계정 보유 여부 | Y/N |
| Cloudflare 플랜 (Free/Pro 등) | |
| 현재 VPS의 IPv4 / IPv6 | |

> 최종 프로젝트명은 **Vaultix**입니다. 실제 도메인 등록은 MVP 배포 직전에 `vaultix` 기반 후보를 우선 확인합니다.

---

## 5. GitHub / CI·CD

| 항목 | 값 |
|---|---|
| GitHub 계정명 (또는 Org) | |
| Vaultix 저장소 | `https://github.com/naral3221-dotcom/vaultix.git` |
| 저장소 가시성 (Private/Public) 선호 | |
| 저장소 분리 vs 모노레포 선호 | 분리 / 모노레포 / 미정 |
| GitHub Actions 사용 의향 | Y/N (대안: GitLab/Drone/Woodpecker 등) |
| Container Registry 선호 | GHCR / Docker Hub / Self-hosted |
| 시크릿 관리 방식 | GH Actions Secrets / .env / 외부 Vault |

---

## 6. 로컬 개발 환경

| 항목 | 값 |
|---|---|
| OS (Windows / macOS / Linux) | |
| 셸 (bash / zsh / PowerShell) | |
| Node.js 버전 | `node -v` |
| 패키지 매니저 (npm / pnpm / yarn / bun) | |
| Python 버전 | `python --version` |
| Python 가상환경 도구 (venv / uv / poetry) | |
| Docker Desktop 또는 WSL2 Docker | |
| 에디터/IDE | VSCode / Cursor / JetBrains 등 |

---

## 7. 기 보유 라이선스 / 외부 서비스 구독

해당하는 항목에 ✅ 표시하고, 키 보유 여부와 월 한도를 적어주세요.

| 서비스 | 보유 | 플랜/한도 | 비고 |
|---|---|---|---|
| OpenAI API | | | 메타 콘텐츠 LLM 후보 |
| Anthropic API | | | |
| Google Gemini API | | | |
| DeepL API | | | i18n 번역 |
| Cloudflare R2 | | | 50GB 도달 시 이전 |
| Cloudflare Turnstile | | | 봇 차단 |
| Resend | | | 트랜잭션 이메일 |
| Sentry | | | 에러 추적 |
| Plausible (호스티드) | | | 자체호스팅 예정이면 N |
| 도메인 등록 대행사 계정 | | | |
| Nanobanana API | | | 이미지 생성 1차 |
| OpenAI `gpt-image-2` | | | 이미지 생성 2차 |
| Hugging Face | | | 모델 다운로드 |

## 7.1 로컬 LLM 확인값

| 항목 | 값 |
|---|---|
| 로컬 모델 폴더 | `C:\AI\llm` |
| 저장소 성격 | Ollama가 아니라 GGUF/LM Studio 계열 |
| 확인 모델 | Qwen3.6 27B/35B GGUF + mmproj |
| MVP 사용 여부 | 기본 라우팅에는 넣지 않음. 보조/실험용으로만 기록 |

---

## 8. 보안 / 백업 정책

| 항목 | 값 |
|---|---|
| 현재 백업 정책 (있다면) | |
| 외부 백업 저장소 | 예: Backblaze B2, Wasabi, S3, 없음 |
| SSH 키 관리 방식 | |
| Secrets 노출 사고 경험 | Y/N (있다면 어떻게 대응했는지 한 줄) |

---

## 9. 그 외 알려두고 싶은 제약/선호

자유 서술로:

- 본업 일정 상 작업 가능한 시간대 (예: 평일 밤 22~24시, 주말 오전):
- 절대 피하고 싶은 기술 스택이 있다면:
- 반드시 쓰고 싶은 도구/언어가 있다면:
- 기타 운영자 입장에서 미리 알려야 할 사항:

---

## 작성 완료 체크

- [ ] 1~9번 항목 채움 (모르는 항목도 "모름"으로라도 표시)
- [ ] 코워크 채팅에 "체크리스트 채웠어" 메시지 보냄
- [ ] (병행) `구현시작_README.md`와 `기획서/00_IMPLEMENTATION_SPEC_v0.4.md`를 먼저 전달함
