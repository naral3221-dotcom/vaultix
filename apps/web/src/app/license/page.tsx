import { PublicPageShell } from "../_components/public-page-shell";

export default function LicensePage() {
  return (
    <PublicPageShell
      eyebrow="License"
      title="라이선스"
      description="받은 자산은 평생 자유롭게 사용할 수 있습니다. 단, 파일 자체를 그대로 재배포하는 것은 금지됩니다."
    />
  );
}

